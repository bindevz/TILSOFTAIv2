# Release Evidence Bundles

Release evidence bundles are generated with `tools/evidence/New-ReleaseEvidenceBundle.ps1` and validated with `tools/evidence/Test-ReleaseEvidenceBundle.ps1`.

Each bundle should contain the compatibility inventory, DB major readiness packet, fallback posture, validation results, and certification evidence manifest required by the release review flow.
